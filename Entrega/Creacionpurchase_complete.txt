%sql
CREATE TABLE purchase_complete
USING DELTA
AS 
SELECT
lower(tb.event) as event
,tb.user_id
,date_format(tb.timestamp, 'yyyy-MM-dd HH:mm:ss') as timestamp
,tb.amount
,lower(tb.payment_method) as payment_method
,tb.phone
FROM testdatabricks.default.events_december_2025 AS tb
WHERE 1=1
AND user_id IS NOT NULL
AND user_id <> ''
AND amount IS NOT NULL
AND payment_method IS NOT NULL
AND phone LIKE  '+%'